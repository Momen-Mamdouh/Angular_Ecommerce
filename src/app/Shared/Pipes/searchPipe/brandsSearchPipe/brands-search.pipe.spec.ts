import { BrandsSearchPipe } from './brands-search.pipe';

describe('BrandsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BrandsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
